function updateMarquee(messages){const marqueeContent=document.getElementById('marqueeContent');const marqueeContainer=document.getElementById('marqueeContainer');if(messages&&messages.length>0){const messageText=messages.map(m=>).join('   •   ');marqueeContent.innerHTML=messageText+'   •   '+messageText;marqueeContainer.style.display='block';marqueeContainer.style.animation='fadeInSlide 0.5s ease-out';let currentIndex=0;setInterval(()=>{currentIndex=(currentIndex+1)%messages.length;const singleMessage=;marqueeContent.innerHTML=singleMessage+'   •   '+singleMessage;},30000);}else{marqueeContainer.style.display='none';}}
